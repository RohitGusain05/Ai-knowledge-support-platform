# Database migrations

The authoritative application migrations currently live in:

`backend/src/main/resources/db/migration/`

Flyway applies them in version order.

The root database directory is reserved for database documentation, local tooling, and future infrastructure configuration.
